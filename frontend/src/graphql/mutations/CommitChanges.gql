mutation CommitChanges($input: CommitChangesInput!) {
  commitChanges(input: $input) {
    success
    written
    failed
    errors
    session {
      id
      canCommit
    }
    clientMutationId
  }
}
