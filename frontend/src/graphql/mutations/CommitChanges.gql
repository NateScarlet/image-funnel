#import ../fragments/SessionFragment.gql

mutation CommitChanges($input: CommitChangesInput!) {
  commitChanges(input: $input) {
    success
    written
    failed
    errors
    session {
      ...SessionFragment
    }
    clientMutationId
  }
}
