mutation CommitChanges($input: CommitChangesInput!, $format: ImageFormat = WEBP) {
  commitChanges(input: $input) {
    written
    matched
    session {
      ...Session
    }
    clientMutationId
  }
}
