mutation SetDirectoryState($input: SetDirectoryStateInput!) {
  setDirectoryState(input: $input) {
    id
    state {
      browse {
        filterBy {
          id
          rating
          label
          query
        }
        filterMemoBy {
          id
          hidden
        }
      }
      updatedAt
    }
  }
}
