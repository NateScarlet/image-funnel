#import '../fragments/SessionFragment.gql'

mutation CreateSession($input: CreateSessionInput!) {
  createSession(input: $input) {
    session {
      ...SessionFragment
    }
    clientMutationId
  }
}
