query GetDirectories($id: ID, $includeStats: Boolean = true) {
  directory(id: $id) {
    ...Directory
    stats @include(if: $includeStats) {
      ...DirectoryStats
    }
    directories {
      ...Directory
      stats @include(if: $includeStats) {
        ...DirectoryStats
      }
    }
  }
}
