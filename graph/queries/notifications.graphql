extend type Query {
  """
  查询通知列表，采用 Relay Connection 分页。
  channel 可作为 filterBy 中的一项筛选，不传则获取所有频道的通知。
  """
  notifications(
    "筛选条件"
    filterBy: NotificationFilters
    first: Int = 50
    after: String
  ): NotificationConnection!
}
