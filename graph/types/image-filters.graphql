type ImageFilters
  @goModel(model: "main/internal/application/image.ImageFilters") {
  rating: [Int!]
}

input ImageFiltersInput
  @goModel(model: "main/internal/application/image.ImageFilters") {
  rating: [Int!]!
}
