type Image @goModel(model: "main/internal/shared.ImageDTO") {
  id: ID!
  filename: String!
  size: Int!
  url(width: Int, quality: Int): URI!
  modTime: Time!
  width: Int!
  height: Int!
  currentRating: Int
  xmpExists: Boolean!
}
