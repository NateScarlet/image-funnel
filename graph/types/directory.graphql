type Directory @goModel(model: "main/internal/shared.DirectoryDTO") {
  id: ID!
  parentId: ID
  path: String!
  root: Boolean!
  imageCount: Int!
  subdirectoryCount: Int!
  latestImage: Image
  ratingCounts: [RatingCount!]!
  directories: [Directory!]! @goField(forceResolver: true)
}
