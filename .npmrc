# npmmirror 对新增的平台绑定包（@rolldown/*、@oxlint/*）同步滞后导致安装超时，
# 这两个 scope 直连官方源，其余依赖仍走用户默认镜像
@rolldown:registry=https://registry.npmjs.org
@oxlint:registry=https://registry.npmjs.org

# 沙箱环境下 pnpm 的 store 完整性逐包校验（found_in_store）会死锁，关闭后
# 直接使用 store 内容；完整性由内容寻址哈希本身保证，风险可控
verify-store-integrity=false
