id = "retention-rating1"
name = "保留图片(1星)"
description = "会话提交后，仅保留最近的32张1星图片，超出部分移至回收站"
command = "uv run runner.py retention"

[on.post_commit_session]

[env]
HOOK_IMAGE_RATING = "1"
HOOK_MAX_RETAIN = "32"
